import React from "react";

const Title = () => {
  return (
    <div className="title flex">
      <h1>Creating a basic Drag & Drop</h1>
      <span>( without external libraries )</span>
    </div>
  );
};

export default Title;
